#include <bits/stdc++.h>
using namespace std;
int32_t main()
{
    int n;
    cin >> n;
    int a[n];
    for (int ii = 0; ii < n; ii++)
        cin >> a[ii];

    for (int ii = 0; ii < n; ii++)
        cout << a[ii] << ' ';
}