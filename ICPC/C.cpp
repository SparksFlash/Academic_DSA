#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t, sum = 0;
    cin >> t;
    while (t--)
    {
        int n, k;
        cin >> n >> k;
        while (n--)
        {
        }
        cout << sum - n;
    }
}